# PE-ownership literature and references

Recovered from `mufflyt/isochrones`, where they were deleted by commit
`c426bcaed` (2026-06-22, claimed to add `set -eo pipefail` to six shell
scripts but instead deleted 24,506 of 24,514 tracked files). Restored via
`mufflyt/private_equity` branch `import/isochrones-pe-assets` (commit
`99acdb7`), verified byte-identical against that branch's blobs.

- `references/references.bib`, `references/private_equity_ownership_references.bib`,
  `references/private_equity_physician_directory_references.bib` — back
  `PRIVATE_EQUITY_OWNERSHIP_METHODS.md` and
  `PRIVATE_EQUITY_PHYSICIAN_DIRECTORY_METHODS.md`, which remain in
  `isochrones` and were not pulled over.
- `pdfs/*.pdf` — copyrighted published articles, retained as personal
  research copies.
