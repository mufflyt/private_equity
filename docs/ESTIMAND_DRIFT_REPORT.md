# Estimand drift report

Generated by `R/estimand_drift.R` on 2026-09-05. Do not hand-edit.

Compares three declarations of the same quantities: what `SAP.lock` names, what
`primary_analysis.Rmd` reports, and what the manuscript prints. It reads declarations
only, fits nothing, and needs no cohort data.

## Prespecified analyses

| Analysis | Estimand | Reporting scale | Family | Subset |
|---|---|---|---|---|
| `obtainment_primary` | `pe` | odds ratio | binomial | medicaid == 1 |
| `waittime_primary` | `pe:medicaid` | incidence rate ratio | nbinom2 | obtained == 1 |
| `obtainment_secondary` | `pe:medicaid` | odds ratio | binomial | - |
| `access_unconditional` | `pe` | difference in cumulative access probability at the horizon | - | medicaid == 1 |

## Scale labels printed in the manuscript

| Label | Value | Scale it denotes |
|---|---|---|
| OR | [0.26] | odds ratio |
| IRR | [1.31] | incidence rate ratio |
| IRR | [1.31] | incidence rate ratio |

## Declared derived quantities

Readings of a prespecified model that the plan does not name as its own analysis.
Declaring one is what separates it from an unplanned comparison, which would look identical.

| Reported as | Derived from | Term | Scale | Justification |
|---|---|---|---|---|
| commercial-arm ownership contrast | obtainment_secondary | `pe` | odds ratio | The secondary obtainment model is obtained ~ pe * medicaid + ..., fitted over both payers. At medicaid = 0 the pe main effect IS the ownership contrast among commercial calls. It is a reading OF a prespecified model, not a new analysis, so SAP.lock correctly does not name it -- and declaring it here is what separates it from an unplanned comparison, which would look identical in the output. |

## Findings

No drift detected: every scale the manuscript prints is one a prespecified or declared-derived analysis uses.
